# 原型数据与隐私边界

## 训练内容与外部服务

- 用户语音会直接发送给 ElevenLabs Realtime Speech-to-Text，用于当前回答的实时转写。
- 用户在首页明确开启“在此设备保存我的录音”后，同一麦克风流还会在浏览器内生成压缩录音；停止录音只形成临时数据，确认正式回答或重做第二稿后才写入独立本机 IndexedDB。录音 Blob 不发送给 Kaiwa Worker、OpenAI-compatible 模型或匿名技术验证端点。
- AI 日语文本会直接发送给 ElevenLabs Text-to-Speech WebSocket，用于生成当前台词语音。
- 用户确认后的转写、有限对话历史、场景信息和反馈/重做请求会通过同站点 Cloudflare Worker 发送给 OpenAI-compatible 模型服务，用于生成相手回复、提示、听力支架与会后反馈。
- OpenAI-compatible 请求设置 `store: false`，不使用 Conversations 持久化。ElevenLabs 与模型供应商自身的安全、诊断、账户日志及保留能力仍以对应账户设置、套餐和服务条款为准；本原型不承诺供应商零保留。

## 自动匿名技术验证

- 测试使用期间，产品会自动记录并发送匿名技术验证数据，不显示遥测控制入口或状态入口。训练不依赖遥测，收集、浏览器存储或网络传输失败都不会阻断训练。
- 技术指标只包含固定形状的流程阶段、耗时、失败类别、重试、功能使用计数、粗粒度设备/系统/浏览器/网络类别、匿名会话标识、客户端标识的服务端 HMAC 派生值、不含对话文本的会话/回合/复盘结果枚举，以及模型输入/输出 token 的数量统计。wire schema 版本为 2，批次使用 `anonymousMetricsCollection: true` 表示自动技术验证；token 数量只反映调用规模，不是访问凭据。
- 技术指标不会包含原始录音、转写文本、对话内容、中文场景描述、访问令牌、API Key、IP 地址字段、User-Agent、完整浏览器标识、邮箱、姓名、账号或模型自由文本评价。Cloudflare 平台可能按其自身配置处理网络层 IP 和诊断日志，本应用 D1 不写入 IP 字段。
- 浏览器使用独立 IndexedDB `kaiwa-validation-telemetry` 暂存待发送的固定事实、随机会话 ID、尝试次数与重试时间；它不与本机练习历史混用。浏览器 outbox 不保存 telemetry token，真正的遥测 token 仅保存在当前页面内存，不进入 outbox、会话快照、完成复盘恢复记录或练习历史。服务端只保存匿名客户端标识的 HMAC 派生值。
- D1 `VALIDATION_DB` 中的事件、回合和复盘记录保留 180 天；会话汇总保留 365 天，定时清理每天执行。遥测上传凭据按单次会话绑定，不能用于读取数据或调用其他训练接口。已有队列中的旧 wire schema v1 批次可以被拒绝并自然丢弃，不影响训练。

## 浏览器本地数据

- 当前会话恢复使用 `sessionStorage`；场景草拟和完成复盘任务恢复使用 `localStorage`；本机练习历史、可选本机录音和匿名验证 outbox 分别使用独立 IndexedDB。它们不构成账号、云同步或跨设备备份。
- 本机练习历史保存完整场景、长期复练凭据、报告和反馈，但不保存录音 Blob、临时会话 token、遥测 token 或 ElevenLabs 访问 token。
- 本机录音默认关闭。用户明确开启后，只保存已确认的正式回合录音和已确认的重做录音，以及会话 ID、回合、类型、创建时间、时长和 MIME 等定位/播放元数据；不保存转写、场景、报告、反馈或访问凭据。重录、切换文字、取消、转写失败或离开页面会丢弃未确认录音。
- 本机录音只保留最新二十个有录音的会话。完成页和首页历史只在录音实际存在时按点击懒读取；停止、播放结束、替换播放或离开页面后会释放临时播放 URL。删除某场景会删除其关联录音；清除站点数据会删除所有本机记录。关闭保存只阻止之后的录音，不自动删除此前已确认保存的录音。
- MediaRecorder、IndexedDB、存储容量或写入失败只产生非阻断提示，不影响训练、转写确认或重做比较。
- Worker 不持久化完整对话、原始录音、本机录音 Blob、永久 API Key 或临时训练 token；Cloudflare Workflow 在任务有效期内保存场景草拟或反馈任务状态与结果。

## 使用提醒

- 测试时不要说出密码、身份证件号码、住址、医疗隐私或其他敏感信息。
- 永久密钥只允许配置为 Cloudflare Worker Secrets：`ELEVENLABS_API_KEY`、`OPENAI_API_KEY`、`SCENARIO_SIGNING_SECRET`。
